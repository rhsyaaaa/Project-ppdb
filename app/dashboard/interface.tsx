/* eslint-disable @typescript-eslint/no-unused-vars */
interface GenderChartProps {
  data: {
    lakiLaki: number;
    perempuan: number;
  };
}

const GenderChart = ({ data }: GenderChartProps) => {
  return <div>GenderChart</div>;
};

export default GenderChart;